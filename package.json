{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "predeploy": "vite build"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.4.5"
  }
}
